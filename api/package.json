{
  "name": "baud-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute baud-db --local --file ./migrations/0001_init.sql",
    "db:init:remote": "wrangler d1 execute baud-db --remote --file ./migrations/0001_init.sql",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
